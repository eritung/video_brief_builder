# V2.1 Test Plan

## 核心邏輯

1. A：`我喜歡蘋果、香蕉、水蜜桃`；B 第一段：`我喜歡蘋果、香蕉`；B 下一段：`水蜜桃很好吃`。
   - Step 3 不得把 `水蜜桃` 標成部分移除。
   - 若有差異提示，應偏向「分段差異／仍存在於 B 文本」。

2. A：`我喜歡蘋果、香蕉、水蜜桃`；B：`我喜歡頻果、香蕉`。
   - Step 3 應顯示 `我喜歡蘋果、香蕉`。
   - PPTX 應顯示完整 A 原句，並將 `水蜜桃` 紅色刪除線。
   - 自動需求應包含：`移除部分文字：「水蜜桃」`。

3. A/B cue 切法不同但全文一致。
   - 不得產生「移除此段」或「移除部分文字」。
   - Step 3 應以 A 校正後文字顯示。

4. B 將 A 後段搬到前面。
   - 若來源 A 與前後段距離很近，應視為近距離分段差異／自動整合，不產生「移過來」需求。
   - 若來源 A 距離很遠，才應標示順序調換。
   - 遠距離搬移的自動需求需包含來源 A 時間碼。
   - PPTX 不顯示 B 時間碼。

5. A 有一整段在 B 全文完全不存在。
   - 應產生「移除此段」。
   - 時間碼以 A 來源為準。

## 互動與輸出

6. 中文輸入法選字按 Enter 不應誤拆段。
7. 分段後確認、進入需求編輯頁，文字不得又回到 B 錯字版本。
8. 匯出 PPTX 後，A 時間碼、狀態、需求文字需可讀，不得互相重疊。
9. PPTX 只使用 A 時間碼作為操作依據，不輸出 B 時間軸。
10. JSON 仍可作為專案備份重新匯入。

## V2.1 新增測試

11. 低分但唯一最佳匹配：最佳 A 候選若明顯勝過第二名，Step 3 應直接套用 A 文字，不顯示為低信心錯字。
12. 近距離整合：A 相鄰 cue 被 B 合成一句時，不應產生「順序調換」需求。
13. 遠距離搬移：A 後方遠距離段落被插入 B 前方時，應標為順序調換並帶來源 A 時間碼。
