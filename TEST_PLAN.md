# V1.1 Test Plan

1. A 正確、B 有少量錯字：結果仍顯示 A 文字，不把錯字寫回主文。
2. B 把同一句拆成不同字幕 cue：仍應配對到 A 的同一台詞區塊。
3. B 移除 A 的完整片段：標示「移除」，文字使用 A。
4. B 把 A 片段移到前／後：標示「順序調換」。
5. B 有 A 找不到的新內容：標示「疑似新增」，要求人工確認。
6. A/B 差異過大但疑似同句：標示「需確認」，不自動改寫 A。
7. 1440 / 1024 / 768 / 手機寬度：狀態文字、合併／拆分按鈕、換頁 checkbox 不互相遮擋。
8. 頂部永遠可看到「匯出簡報 (.pptx)」；JSON 明確標示為專案備份。
9. 匯出 PPTX 後可上傳 Google Drive 並用 Google 簡報開啟。
10. 超連結在 PPTX 中可點擊。
