# V1.4 Test Plan

## B 分段流程

- [ ] 匯入 A、B 後，先進入「重剪版 B 台詞分段」而不是 A。
- [ ] `Enter` 可在游標處拆成下一段。
- [ ] 拆段後畫面維持在原本操作位置，不跳回頁面上方。
- [ ] 段落開頭按 `Backspace` 可與上一段合併。
- [ ] `Shift + Enter` 只在同段換行。
- [ ] `⌘/Ctrl + Enter` 可切換「這句另起一頁」。
- [ ] 滑鼠的「合併上一段／下一段」仍可使用。

## A 校正／B 時間軸

- [ ] B 有少量錯字、A 正確：正式需求頁使用 A 文字。
- [ ] 高信心匹配時，分段頁可直接看到「A 已校正」。
- [ ] 中等信心匹配時顯示「A 可能對應」，不強制覆寫。
- [ ] B 的時間碼是需求頁與 PPTX 的主要時間碼。
- [ ] PPTX 頁面依 B 時間由前往後排序。
- [ ] B 對 A 的片段順序改動可標示「順序調換」。
- [ ] A 有、B 無的內容標示「移除」，並盡量插在新版上下文附近。
- [ ] B 有、A 找不到的內容標示「疑似新增」。

## 簡報／UI

- [ ] 「另起一頁」在 PPTX 中確實換頁。
- [ ] 「在此後插入新頁」建立獨立頁。
- [ ] REF 超連結在 PPTX 中可點擊。
- [ ] 1440 / 1024 / 768 / 手機寬度下，文字與按鈕不互相遮擋。
- [ ] 頂部可直接匯出 `.pptx`；`.json` 明確僅作專案備份。
- [ ] PptxGenJS 與 JSZip 均從 `vendor/` 正確載入。


## V1.4 新增測試
1. 使用注音／倉頡／拼音等中文輸入法輸入文字，按 Enter 確認候選字，不應拆段。
2. 非輸入法組字狀態下按 Enter，應正常拆段。
3. 拆段後按「復原上一個分段」，應恢復拆段前內容。
4. 匯出 PPTX，確認時間碼、灰色輔助文字、需求文字皆較 V1.3 明顯放大。
5. 投影片右上角不應再出現 AUTO VIDEO BRIEF。
